# Runtime + test dependencies for the Stack Overflow 2024 capstone pipeline.
#
# Python: 3.10.11 (see Pipfile `[requires]`).
# Versions are pinned to match `Pipfile.lock` (the original environment lock).
# This file is the canonical source of truth used by `make setup`.
#
#   `Pipfile` / `Pipfile.lock` are kept as the historical dev manifest.
#   `requirements.txt` (this file) is what CI / `make setup` installs.

# --- pipeline runtime ---
numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.9
seaborn==0.13.2
scikit-learn==1.7.2
openpyxl==3.1.5
pillow==12.2.0

# --- notebooks / labs (course material) ---
ipykernel==7.2.0
ipython==8.39.0

# --- web/API labs (course material) ---
flask==3.1.3
requests==2.34.2
bs4==0.0.2
beautifulsoup4==4.14.3

# --- tests ---
pytest==8.3.5
